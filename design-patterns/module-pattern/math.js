export const add = (x, y) => (
  x + y
);

export const multiply = (x, y) => (
  x * y
);

export const subtract = (x, y) => (
  x - y
);

export const square = (x) => (
  x * x
);

export default {
  divide: (x, y) => x/y
}
